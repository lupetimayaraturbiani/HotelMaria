package com.example.HotelMaria.controller;

// This file kept as a placeholder to avoid accidental re-creation of the old controller.
// The original AuthController functionality has been consolidated into `LoginController`.
// To avoid duplicate Spring mappings keep this class non-annotated and inert.

public final class AuthController {
    private AuthController() {
        // prevent instantiation
    }
}
